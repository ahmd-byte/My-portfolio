Update the `profile` object at the top of `src/App.jsx` before publishing:

- `email`
- `github`
- `linkedin`
- `resume`

Update each project's `caseStudy` and `github` links too.

This component expects:

- React
- Tailwind CSS
- `framer-motion`
- `lucide-react`
